# Intentional fake credentials live in test fixtures and the labeled benchmark
# corpus (they exercise the scanner's own secret-detection rules). Suppress
# secret-scanning ALERTS on those paths; real source is still scanned.
paths-ignore:
  - "benchmark/**"
  - "test/**"
  - "examples/**"
