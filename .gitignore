node_modules/
dist/
*.log
.DS_Store
coverage/
.env
.env.*
*.tsbuildinfo
# MCP Trust Checker writes an integrity lockfile; users decide whether to commit it.
# We ignore it in this repo's own runs, but recommend users COMMIT theirs.
/scratch/

# ephemeris/data artifacts written by scanned servers during live testing
*.bsp
*.sqlite
test.db

# Stray logs / scratch / scan artifacts (never commit)
err.txt
warn.txt
/temp/
*.tgz
